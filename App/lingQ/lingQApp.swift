import SwiftUI
import SwiftData

@main
struct lingQApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
        // TODO: Task 7 — add .modelContainer(for: [Course.self, Word.self])
    }
}
